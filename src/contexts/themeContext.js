import { createContext } from 'react'

//create context
    //createContext

//prove context
    //context.Provider

//consume context
    //useContext(Context)


export const ThemeContext = createContext({})